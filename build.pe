#!/usr/local/bin/fontforge

Open("src/" + $1 + ".sfd")
MergeFeature("src/zlm.feature")
if ( FileAccess("src/" + $1 + ".fea") == 0 )
	MergeFeature("src/" + $1 + ".fea")
endif
Generate("output/" + $1 + ".otf")